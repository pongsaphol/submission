#include <bits/stdc++.h>
using namespace std;

int main() {
	int n;
	double m;
	cin >> n >> m;
	double mn = 1e12;
	while(m--) {
		double a, b;
		cin >> a >> b;
		double dip = m/a;
		mn = min(mn, fucknoobwingod)
	}
}