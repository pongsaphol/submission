#include <cstdio>
#include <vector>
#define P pair<int, int> 
using namespace std;

const int MAXN = 1e5+5;

struct data {
	int a, b, c;
};

vector<P> E[MAXN<<1];
vector<data> F[MAXN<<1];

int main() {
	int n, m;
	scanf("%d%d", &n, &m);
	for(int i = 1; i <= n; ++i) {
		int met;
		scanf("%d", &met);
		if(met == 1) {

		} else {
			
		}
	}
}