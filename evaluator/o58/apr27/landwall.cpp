#include <bits/stdc++.h>
#define long long long
using namespace std;

const int N = 1e3+5;

int n, k, A[N];
long dp[N][N], pref[N][N];

int main() {
	scanf("%d %d", &n, &k);
	for(int i = 1; i <= n; ++i) scamf("%d", A+i);
}