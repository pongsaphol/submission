#include <bits/stdc++.h>
using namespace std;

char A[50];

int main() {
    freopen("filename.txt", "r", stdin);
    srand(time(NULL));
    while(scanf("%s", A) != EOF) printf("%s,%d,%d\n", A, 200, 200);
}
