#include <bits/stdc++.h>
using namespace std;

int n, m, sx, sy, tx, ty, b, t;

int solve() {

}

int main() {
	#ifdef INPUT
	freopen("r", "r", stdin);
	#endif
	int T; scanf("%d", &T);
	while(T--) printf("%d\n", solve());
}