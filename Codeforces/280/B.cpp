#include <bits/stdc++.h>
using namespace std;

const int N = 1e5+5;

int n, pref[N], t[N][2], ptr;

void update(int v) {
	for(int i = 30, ret = 0; ~i; --i) {
		if(t[ret][])
	}
}

int main() {
	scanf("%d", &n);
	update(0);
	for(int i = 1; i <= n; )
}